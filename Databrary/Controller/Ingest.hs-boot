module Databrary.Controller.Ingest where

import Databrary.Model.Id.Types
import Databrary.Model.Volume.Types
import Databrary.Action

viewIngest :: AppRoute (Id Volume)
postIngest :: AppRoute (Id Volume)
